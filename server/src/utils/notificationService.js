// utils/notificationService.js

export const sendNotification = async (userId, message) => {
  // Simulate sending a notification (could be email, push, etc.)
  console.log(`Notification sent to user ${userId}: ${message}`);
};
